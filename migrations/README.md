# YachayBot v2 Migrations

This directory is reserved for v2 database migrations that support the FastAPI service.

The existing Prisma migrations in `prisma/migrations` belong to the current demo application. v2 migrations should recreate the source-grounded search schema from scratch, including documents, chunks, queries, citations, and eval tables.
