# Issue #35 Plan: Add Reproducible Runtime Setup

## Implementation

- Add a Dockerfile for the deterministic Next.js MVP.
- Add a `.dockerignore` file to keep builds focused.
- Document build, run, and validation commands.
- Ensure optional provider keys are not required for baseline runtime behavior.

## Validation

- Run repository validation locally.
- Confirm Docker instructions use the same app scripts.

